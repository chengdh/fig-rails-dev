.table-responsive
  %table.table.table-condensed.table-striped.table-hover
    %tbody
      %tr
        %th 产业类型:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute1)
      %tr
        %th 项目性质:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute2)
      %tr
        %th 项目分类:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute3)
      %tr
        %th 所属二级单位:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute4)
      %tr
        %th 批复单位:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute5)
      %tr
        %th 改造维修对象:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute26)
      %tr
        %th 自行安排父项目:
        %td=@cux_pm_pre_project.try(:cux_pm_pre_option).try(:attribute28)
 
